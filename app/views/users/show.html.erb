<p id="notice"><%= notice %></p>

<h1><%= if @user[:admin] ? 'Manage User' : 'My Account'</h1> %>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<% if user_signed_in? %>
<%= link_to 'Edit', edit_user_path(@user) %> |
<%= end %>
<%= link_to 'Back', users_path %>
